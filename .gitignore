# Secrets — never commit
.env
.env.local
.env.*.local
*.env
!.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# macOS
.DS_Store

# IDE
.vscode/
.idea/
*.swp

# Local data — too large or too dynamic for git.
# bsa.db (SQLite, ~22MB) IS tracked intentionally for demo reproducibility.
# LanceDB is NOT uploaded to Railway (too big for CLI upload) — the container
# generates it from SQLite on first boot into a persistent volume.
data/db/*.db-journal
data/db/lancedb/
data/db/bsa_reviews.db
# Vercel
frontend/.vercel/
